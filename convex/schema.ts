import { defineSchema } from "convex/server";

// This is a placeholder schema for Phase 1 setup
// The actual schema will be implemented in Phase 2
const schema = defineSchema({});

export default schema;
